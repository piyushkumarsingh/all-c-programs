#include<stdio.h>
#include<conio.h>
  main()
  {
  
	float r,area;
	printf("enter the radius of a circle");
	scanf("%f",&r);
	area=3.14*r*r;
	printf("the area of the circle is:",area);
	getch();
	return (area);
   } 
