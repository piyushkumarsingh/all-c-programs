#include<stdio.h>

void main()
{
	char str[500];
	
	printf("Enter a sentence ");
	gets(str);
	
	word(str);
}

void word(char str[])
{
	int i;
	while(str[i]!='\0')
	{
		if(str[i]==' ')
		printf("\n");
		else
		printf("%c",str[i]);
		i++;
	}
}
